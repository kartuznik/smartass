services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    command: ["python", "-m", "bot.main"]
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./docs:/app/docs
    restart: unless-stopped
