"""Placeholder test module intentionally skipped until implementation is added."""

import pytest

pytestmark = pytest.mark.skip(
    reason="Placeholder module: document processor tests will be added with feature scope."
)
